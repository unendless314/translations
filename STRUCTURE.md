# 專案結構說明

## 📁 目錄組織

本專案採用清晰的輸入/輸出分離結構，方便管理翻譯工作流程。

### 目錄用途

```
translations/
│
├── docs/                          # 📖 文檔目錄
│   ├── README.md                  # 完整技術文檔（工具原理、參數調整等）
│   └── QUICKSTART.md              # 快速開始指南（新手必看）
│
├── tools/                         # 🛠️ 工具腳本目錄
│   ├── srt_preprocessor_v2.py     # SRT 前處理工具
│   └── align_timestamps_v2.py     # 時間軸對齊工具
│
├── input/                         # 📥 輸入目錄
│   └── raw/                       # 原始 SRT 字幕檔存放處
│       └── .gitkeep               # （保留目錄結構用）
│
├── output/                        # 📤 輸出目錄
│   ├── preprocessed/              # 前處理後的待翻譯稿
│   │   └── .gitkeep
│   ├── translated/                # 翻譯完成的中英對照檔
│   │   └── .gitkeep
│   └── final/                     # 最終成品（帶時間軸）
│       └── .gitkeep
│
├── archive/                       # 🗄️ 歸檔目錄
│   └── .gitkeep                   # 已完成專案可移至此處
│
├── README.md                      # 專案首頁（快速導覽）
├── .gitignore                     # Git 忽略規則
└── STRUCTURE.md                   # 本文件
```

---

## 🔄 檔案流動路徑

### 完整工作流程

```
1️⃣ input/raw/your-subtitle.srt
   │
   │ [執行 srt_preprocessor_v2.py]
   ↓
2️⃣ output/preprocessed/your-subtitle_待翻譯_簡單版.md
   │
   │ [人工或 AI 翻譯]
   │ [翻譯完成後移動檔案]
   ↓
3️⃣ output/translated/your-subtitle_中英對照.md
   │
   │ [執行 align_timestamps_v2.py]
   ↓
4️⃣ output/final/your-subtitle_中英對照_有時間軸.md
   │
   │ [專案完成，可選擇歸檔]
   ↓
5️⃣ archive/project-name/
```

---

## 📝 檔案命名建議

### 一致的命名規範

```
原始檔案:
  input/raw/S01-E12-subtitle.srt

前處理產出:
  output/preprocessed/S01-E12_待翻譯_簡單版.md
  output/preprocessed/S01-E12_待翻譯_結構化版.md

翻譯完成:
  output/translated/S01-E12_中英對照.md

最終成品:
  output/final/S01-E12_中英對照_有時間軸.md
```

### 建議格式
- 使用 `S01-E12` 表示季數和集數
- 使用底線 `_` 分隔不同部分
- 中文使用清楚的描述（待翻譯、中英對照等）

---

## 🗂️ 歸檔建議

完成專案後，可以將所有相關檔案移到 `archive/` 目錄：

```bash
# 建立專案資料夾
mkdir -p archive/S01-E12-ProjectName

# 移動所有相關檔案
mv input/raw/S01-E12*.srt archive/S01-E12-ProjectName/
mv output/preprocessed/S01-E12* archive/S01-E12-ProjectName/
mv output/translated/S01-E12* archive/S01-E12-ProjectName/
mv output/final/S01-E12* archive/S01-E12-ProjectName/
```

這樣可以保持工作目錄整潔，同時保留完整的專案歷史。

---

## 🔒 Git 版本控制

### .gitignore 設定

專案已設定 `.gitignore`，自動忽略：
- ✅ 所有輸入檔案（`input/raw/*.srt`）
- ✅ 所有輸出檔案（`output/` 下的所有 .md）
- ✅ 歸檔檔案（`archive/*/`）
- ✅ Python 快取檔案
- ✅ 編輯器臨時檔案

### 版本控制內容

只追蹤：
- ✅ 工具腳本（`tools/*.py`）
- ✅ 文檔（`docs/*.md`、`README.md`）
- ✅ 目錄結構（`.gitkeep` 檔案）
- ✅ Git 設定（`.gitignore`）

這樣可以確保：
1. 工具和文檔有版本控制
2. 個人翻譯檔案不會被提交
3. 團隊協作時不會衝突

---

## 💡 使用技巧

### 批次處理多個檔案

如果有一整季的字幕要處理：

```bash
# 1. 將所有 SRT 放到 input/raw/
cp Season01/*.srt input/raw/

# 2. 逐一處理（編輯腳本設定檔案名稱）
python3 tools/srt_preprocessor_v2.py

# 3. 翻譯完成後批次移動
mv output/preprocessed/S01-E*簡單版.md output/translated/

# 4. 批次對齊
# （需要逐一編輯腳本，或考慮開發批次處理工具）
```

### 保持目錄整潔

定期清理：
- 刪除不需要的 `*_結構化版.md`（如果只用簡單版）
- 完成專案後移至 `archive/`
- 定期檢查 `output/` 目錄，避免檔案堆積

---

## 📊 目錄狀態檢查

快速檢查各目錄的檔案數量：

```bash
# 原始 SRT 數量
ls input/raw/*.srt | wc -l

# 待翻譯數量
ls output/preprocessed/*.md | wc -l

# 翻譯完成數量
ls output/translated/*.md | wc -l

# 最終成品數量
ls output/final/*.md | wc -l
```

---

**建議：** 初次使用時，先用一個小檔案測試整個流程，熟悉後再處理大量檔案。

**最後更新：** 2025-10-07
